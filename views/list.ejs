<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <form action="/" method="GET">
        <button type="submit">Home</button>
    </form>

    <span>
        <% if(friend.length == 0) { <p>The database is empty</p>} %>
        <% for(let i=0; i < friend.length; i++) {%>
            <li class="friend" style="list-style-type:none">
                
                <span><%= friend[i].friendName %></span>
                <span><%= friend[i].birthday %></span>

            </li>
        <% } %>
    </span>
    
</body>
</html>